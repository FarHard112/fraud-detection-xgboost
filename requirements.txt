Flask==2.3.3
flask-restx==1.3.0  # Adjust based on compatibility; you mentioned 1.2.0, which should be fine if that's what you're using
pandas==1.3.3       # Use the latest 1.x version available or the specific version you need
numpy==1.21.2
scikit-learn==0.24.2
xgboost==2.0.3
